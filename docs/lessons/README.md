# Lessons (incident postmortems)

One dated file per production incident, moved verbatim out of AGENTS.md
on 08-07-2026 (the standing RULES each one produced remain in AGENTS.md's
hard-won-lessons section, which links here). Add new postmortems as
`YYYY-MM-DD-<slug>.md` and add a one-line rule + link to AGENTS.md.
